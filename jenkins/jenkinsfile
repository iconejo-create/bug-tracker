pipeline {
    agent any

    tools{
        go '1.26.2'
    }

    stages {
        stage('Unit-test Backend') {
            steps {
                dir('bugtracker-backend'){
                    sh 'go test -v ./...'
                }


            }
        }
    }
}
