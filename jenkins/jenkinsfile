pipeline {
    agent any

    stages {
        stage('Unit-test Backend') {
            steps {
                dir('butracker-backend'){
                    sh 'go test -v ./...'
                }


            }
        }
    }
}
